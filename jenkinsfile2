pipeline{
    agent any
    environment{
      dockerImage = ''
      registry = 'mmaaasshhaaaaa/takenote'
    }
    stages{
        stage('Build')
        {
            steps{
              script{
                dockerImage = docker.build registry
              }
            }
        }
    }
}
