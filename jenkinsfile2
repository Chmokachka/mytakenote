pipeline{
  agent {
    dockerfile {
      additionalBuildArgs '--build-arg CLIENT_ID=79ccc48a450cd4e391a8'
      args '-t takenode:mytag'
      dir 'build'
      label 'myDocker'
    }
}
    stages{
        stage('Build')
        {
            steps{
              sh 'hello'
            }
        }
    }
}
