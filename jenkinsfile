node {
    stage('Initialize'){
        def dockerHome = tool 'docker-cli-latest'
        env.PATH = "${dockerHome}/bin:${env.PATH}"
    }
    stage('Building image') {
          sh 'docker build -t takenote:mytag .'
          sh 'docker run -p 5000:5000 takenote:mytag'
        }
  }
