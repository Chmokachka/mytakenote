pipeline {
    agent any
    stages {
        stage('hello')
        {
          steps{
            script{
              def scannerHome = tool 'sonar_scanner';
              withSonarQubeEnv('sonar_scanner') {
                  sh "${scannerHome}/bin/sonar-scanner"
            }
          }
        }
    }
  }
}
