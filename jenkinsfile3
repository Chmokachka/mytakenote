pipeline {
    agent any

    stages {
        stage('Ok') {
            steps {
                echo "Ok"
            }
        }
    }
    post {
      always {
          emailext body: "${currentBuild.currentResult}: Job Name:
              ${env.JOB_NAME} || Build Number: ${env.BUILD_NUMBER}\n More
              information at: ${env.BUILD_URL}",
              subject: 'Declarative Pipeline Build Status',
              to: 'karkarech7@gmail.com'
      }
  }
}
